{
  "name": "nivesh-diary",
  "version": "1.0.0",
  "description": "Nivesh Diary - Android PWA App",
  "scripts": {
    "build": "rm -rf www && mkdir -p www && cp index.html manifest.json sw.js www/ && cp -r css js icons assets www/",
    "cap:sync": "cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.1",
    "@capacitor/core": "^6.1.1",
    "@capacitor/filesystem": "^6.0.4",
    "@capacitor/local-notifications": "^6.1.3",
    "@capacitor/share": "^6.0.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.1"
  }
}
